# AgentAPI Proxy Configuration (Session management)
NEXT_PUBLIC_AGENTAPI_PROXY_URL=http://localhost:8081
AGENTAPI_API_KEY=your-api-key-here
AGENTAPI_TIMEOUT=10000

# Authentication Configuration
AGENTAPI_PROXY_URL=http://localhost:8080
COOKIE_ENCRYPTION_SECRET=your-32-byte-hex-secret-here-64-characters-long-for-aes256-gcm

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Environment
NODE_ENV=development

# Login Page Customization (Runtime configuration via /api/config)
# AUTH_MODE: 'oauth_only' | 'api_key' | 'both' (default: 'both')
AUTH_MODE=both
LOGIN_TITLE=AgentAPI UI
LOGIN_DESCRIPTION=Enter your API key or sign in with GitHub to continue.
LOGIN_SUB_DESCRIPTION=API key can be any valid authentication token for your AgentAPI service.

# UI Customization
# FAVICON_URL: Custom favicon URL (optional, e.g., https://example.com/favicon.ico)
# FAVICON_URL=

# Legacy Login Page Customization (deprecated, use above instead)
# These are still supported for backward compatibility
# NEXT_PUBLIC_OAUTH_ONLY_MODE=false
# NEXT_PUBLIC_LOGIN_TITLE=AgentAPI UI
# NEXT_PUBLIC_LOGIN_DESCRIPTION=...
# NEXT_PUBLIC_LOGIN_SUB_DESCRIPTION=...

# VAPID Keys for Push Notifications (Required for push notifications)
# Generate these using: npx web-push generate-vapid-keys
# For container deployment, use VAPID_PUBLIC_KEY (runtime config)
# For build-time deployment, use NEXT_PUBLIC_VAPID_PUBLIC_KEY (build config)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=BOv-qOWAZ4--eLYAQNk-0jZPDGHH3rrmb4RFaQglVpdz_zQrS5wH1puNS4aWoWSDnRbPO764YURRZt8_B2OMkDQ
VAPID_PUBLIC_KEY=BOv-qOWAZ4--eLYAQNk-0jZPDGHH3rrmb4RFaQglVpdz_zQrS5wH1puNS4aWoWSDnRbPO764YURRZt8_B2OMkDQ
VAPID_PRIVATE_KEY=-ni1VcRxrb-o_6h2Sy2TmQyk1iNRJCCBcqZXgKu94Zk
VAPID_SUBJECT=mailto:admin@agentapi.example.com