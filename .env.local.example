# Single Profile Mode Configuration
# Set to 'true' to enable single profile mode
SINGLE_PROFILE_MODE=true
NEXT_PUBLIC_SINGLE_PROFILE_MODE=true

# Cookie encryption secret for Single Profile Mode
# Generate a secure 32-byte key in hex format (64 characters)
# Example: openssl rand -hex 32
COOKIE_ENCRYPTION_SECRET=your_64_character_hex_string_here

# AgentAPI Proxy URL (optional, defaults to http://localhost:8080)
# AGENTAPI_PROXY_URL=http://localhost:8080