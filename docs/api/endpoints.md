# エンドポイント

利用可能な API エンドポイント一覧。

## セッション管理

- `GET /api/sessions` - セッション一覧取得
- `POST /api/sessions` - セッション作成

## Webhook 管理

- `GET /api/webhooks` - Webhook 一覧取得
- `POST /api/webhooks` - Webhook 作成
